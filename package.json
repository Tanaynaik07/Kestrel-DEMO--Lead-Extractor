{
  "name": "lead-extractor",
  "version": "1.0.0",
  "description": "Landing page + Node backend that turns a free-text inquiry into a structured CRM lead: Gemini extracts the fields, they're appended to a Google Sheet, and a notification email is sent.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.5.1",
    "googleapis": "^144.0.0",
    "helmet": "^8.3.0"
  },
  "engines": {
    "node": ">=18"
  }
}